
<%= simple_form_for(@sales_term) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.association :sale %>
    <%= f.association :term %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

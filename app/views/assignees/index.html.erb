<p id="notice"><%= notice %></p>

<h1>Assignees</h1>

<table>
  <thead>
    <tr>
      <th>Assignement</th>
      <th>Team</th>
      <th>Role</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @assignees.each do |assignee| %>
      <tr>
        <td><%= assignee.assignement_id %></td>
        <td><%= assignee.team_id %></td>
        <td><%= assignee.role_id %></td>
        <td><%= link_to 'Show', assignee %></td>
        <td><%= link_to 'Edit', edit_assignee_path(assignee) %></td>
        <td><%= link_to 'Destroy', assignee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Assignee', new_assignee_path %>
